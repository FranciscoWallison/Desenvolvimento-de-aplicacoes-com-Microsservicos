# 🧪 Laboratório Prático: Liberando o Acesso WAF → Server e Testando `site.lab`

> **Tema:** Criar a regra de firewall **WAF → server** (DMZEXT), resolver **`site.lab`** via `/etc/hosts` e **testar a aplicação** ponta a ponta
> **Pré-requisitos:** [84.md](84.md) (proxy reverso `site.lab_conf`), [83.md](83.md) (Nginx no server)
> **Conceitos base:** [69.md](69.md) (Block Geral / direção das regras), [76.md](76.md) (NAT 1:1 do WAF), [66.md](66.md) (resolução de nomes)
> **Curso 2 — Proxy Reverso, SSH e DNS**

---

## 🎯 Objetivo do Lab

No [84.md](84.md) configuramos o WAF para **encaminhar** (`proxy_pass`) ao servidor `172.100.1.100`. Mas faltam **duas peças** para funcionar de verdade:

1. **Liberar no firewall** o tráfego **WAF → server** (a regra que faltava — lembra do "502 Bad Gateway" do [84.md](84.md)?)
2. Fazer o navegador **resolver `site.lab`** para o IP público do WAF (via `/etc/hosts`)

E então **testar** ponta a ponta, observando o **log com o IP real** — a prova de que os headers do [84.md](84.md) funcionam.

---

## 🔓 Etapa 1 — Regra de firewall: WAF → Server

O WAF (`172.100.2.100`, **DMZEXT**) precisa alcançar o server (`172.100.1.100`, DMZ) na porta 80 — mas o **Block Geral** ([69.md](69.md)) barra. Criamos a exceção **na interface DMZEXT** (onde o tráfego do WAF **entra** no firewall).

No pfSense (`https://192.168.56.10`):

```
Firewall → Rules → DMZEXT
```

**↑ Add** (verde) e configure:

```yaml
Action: Pass
Disabled: <vazio>
Interface: DMZEXT
Address Family: IPV4
Protocol: TCP

Source: Single host - 172.100.2.100        # WAF
Destination: Single host - 172.100.1.100    # server
Destination Port Range: From 80(HTTP) to 80(HTTP)

Log: ✓ check
Description: Acesso WAF > SITE
```

**Save** → **Apply Changes**.

### Por que cada valor

| Campo | Por quê |
|-------|---------|
| **Interface: DMZEXT** | Onde o WAF está — o tráfego dele **sai** por essa interface |
| **Source: 172.100.2.100** | Só o **WAF** pode iniciar essa conexão (menor privilégio) |
| **Destination: 172.100.1.100** | O **server** interno (backend do `proxy_pass`, [84.md](84.md)) |
| **Port: 80 (HTTP)** | O server serve a aplicação em HTTP |
| **Log: ✓** | Auditar o tráfego WAF→backend |

> 🔑 **Essa é a regra que faltava no [84.md](84.md).** Sem ela, o `proxy_pass` do WAF bateria no Block Geral e você veria **502 Bad Gateway** — o proxy recebe, mas não consegue **entregar** ao backend.

---

## 🌐 Etapa 2 — Resolver `site.lab` no `/etc/hosts`

O `server_name site.lab` do [84.md](84.md) só serve para algo se **`site.lab` resolver** para o IP do WAF. Como não temos um DNS registrando esse nome, usamos o **arquivo hosts** da **sua máquina** (resolução local, que tem **prioridade sobre o DNS**).

| SO | Como abrir |
|----|-----------|
| **Windows** | Bloco de Notas **como administrador** → abrir `C:\Windows\System32\drivers\etc\hosts` |
| **Linux/macOS** | `sudo nano /etc/hosts` |

Adicione ao final (com **o seu** IP — veja abaixo):

```text
192.168.100.173 site.lab
```

> 📌 **Qual IP usar?** É o **External IP do NAT 1:1 do WAF** — no pfSense: `Firewall → NAT → 1:1`, o NAT **"Nateando WAF na Internet"** ([76.md](76.md) publicou o WAF assim). **Varia por máquina** — o `192.168.100.173` é só exemplo.

> 💡 **O que é o `/etc/hosts`?** É a forma **mais antiga** de resolução de nomes — um mapa **estático** `IP → nome`, consultado **antes** de perguntar ao DNS ([66.md](66.md)/[82.md](82.md)). Perfeito para "inventar" um nome como `site.lab` só na sua máquina, sem registrar domínio.

---

## 🔬 Etapa 3 — Testar ponta a ponta

### 3.1 — Acompanhar o log no server (em tempo real)

Acesse o server (via jump host, [82.md](82.md)) e rode:

```bash
tail -f /var/log/nginx/access.log
```

O **`tail -f`** (*follow*) fica **transmitindo** as novas linhas do log conforme elas chegam ([71.md](71.md)).

### 3.2 — Acessar a aplicação

No navegador da sua máquina:

```
http://site.lab/
```

A aplicação (servida pelo Nginx do server, via WAF) deve responder.

### 3.3 — O que observar no log 🎯

No terminal do `tail -f`, a requisição aparece **na hora** — e, graças aos headers do [84.md](84.md), com o **IP real** do seu navegador (não o `172.100.2.100` do WAF):

```
<SEU-IP-REAL> - - [.../...] "GET / HTTP/1.1" 200 ... "-" "Mozilla/5.0 ..."
```

> 🔑 **Este é o "momento de virada" do Curso 2:** o navegador fala `site.lab`, o `/etc/hosts` resolve para o WAF, o **NAT 1:1** entrega ao WAF, o WAF (ModSecurity + proxy reverso) **encaminha** ao server, e o server **loga o IP real** do cliente. Todas as peças dos últimos labs funcionando **juntas**.

---

## 🗺️ O caminho completo da requisição

```
🧑 Navegador  ── "site.lab" ──►  /etc/hosts  →  192.168.100.173 (IP público do WAF)
     │
     ▼  http://192.168.100.173
🔥 pfSense (NAT 1:1, 76.md)  ──►  🛡️ WAF/nginx 172.100.2.100:80
                                    ├─ ModSecurity inspeciona (75.md)
                                    └─ proxy_pass → 172.100.1.100 (84.md)
                                         │  (regra DMZEXT deste lab autoriza)
                                         ▼
                                    🖥️ Server nginx 172.100.1.100:80
                                       access.log registra o IP REAL (X-Real-IP)
```

---

## 🐞 Troubleshooting Comum

### "502 Bad Gateway"

Faltou a **regra WAF → server** (Etapa 1) ou o **Nginx do server** não está no ar ([83.md](83.md)). Confirme a regra em `Firewall → Rules → DMZEXT` e `ss -nlt` no server.

### "`site.lab` não abre / vai para outro lugar"

1. A linha no `/etc/hosts` está correta e **salva**? (Windows exige **admin** para salvar)
2. O IP é o **External IP do NAT 1:1** do WAF (`Firewall → NAT → 1:1`)?
3. Limpe o cache DNS do SO (Windows: `ipconfig /flushdns`).

### "Abre, mas o log mostra o IP do WAF, não o meu"

Revise os `proxy_set_header X-Real-IP`/`X-Forwarded-For` no `site.lab_conf` do WAF ([84.md](84.md)) — e configure o server para **ler** esses headers se quiser o IP real no `$remote_addr`.

### "Nada aparece no `tail -f`"

A requisição não chegou ao server (parou no WAF/firewall). Reveja a Etapa 1 e o `proxy_pass` do [84.md](84.md).

---

## 🧠 Conceitos Aprendidos

| Conceito | Definição |
|----------|-----------|
| **Regra WAF → server (DMZEXT)** | Libera o `proxy_pass` a alcançar o backend na porta 80 |
| **`/etc/hosts`** | Mapa **estático** nome→IP, consultado **antes** do DNS ([66.md](66.md)) |
| **NAT 1:1 do WAF** | IP público que entrega ao WAF interno ([76.md](76.md)) |
| **`tail -f`** | Acompanha um arquivo de log **em tempo real** ([71.md](71.md)) |
| **`ipconfig /flushdns`** | Limpa o cache de resolução de nomes (Windows) |
| **IP real no log** | Efeito dos headers `X-Real-IP`/`X-Forwarded-For` ([84.md](84.md)) |
| **502 Bad Gateway** | Proxy não alcançou o backend (regra/serviço faltando) |

---

## ✅ Quiz Mental

1. Por que a regra WAF → server fica na interface **DMZEXT**? O que acontece sem ela?
2. Para que serve a linha no `/etc/hosts`, e por que não precisamos registrar um domínio real?
3. Qual IP colocamos no `/etc/hosts` para o `site.lab` e onde encontrá-lo?
4. O que o `tail -f /var/log/nginx/access.log` mostra durante o teste?
5. Como este lab prova que os headers configurados no [84.md](84.md) funcionaram?

<details>
<summary>Ver respostas</summary>

1. Porque o WAF (`172.100.2.100`) está na **DMZEXT**, e as regras são avaliadas na **interface onde o tráfego entra** no firewall ([69.md](69.md)/[70.md](70.md)). Sem ela, o `proxy_pass` do WAF bate no **Block Geral** e o cliente vê **502 Bad Gateway** — o proxy recebe mas não **entrega** ao backend
2. A linha `192.168.100.173 site.lab` faz a **sua máquina** resolver `site.lab` **localmente**, sem DNS. O `/etc/hosts` é consultado **antes** do DNS ([66.md](66.md)), então podemos "inventar" um nome só para o lab, sem pagar/registrar um domínio real
3. O **External IP do NAT 1:1 do WAF** (o NAT "Nateando WAF na Internet", em `Firewall → NAT → 1:1`, [76.md](76.md)). É o IP **público** que entrega ao WAF interno — **varia** por máquina
4. Mostra as **requisições HTTP em tempo real** que chegam ao server — cada acesso a `http://site.lab/` aparece como uma nova linha no `access.log`, na hora
5. Porque no log do server aparece o **IP real do navegador**, e **não** o `172.100.2.100` do WAF. Isso só acontece porque o WAF envia `X-Real-IP`/`X-Forwarded-For` ([84.md](84.md)) — sem eles, todo acesso pareceria vir do proxy

</details>

---

## 🗺️ Status do Roadmap "Segurança de Rede" Hands-on

### ✅ Curso 1 — Firewall, WAF e SIEM
Labs 1–11 concluídos ([68.md](68.md)–[78.md](78.md)).

### 🚀 Curso 2 — Proxy Reverso, SSH e DNS

| # | Lab | Status |
|---|-----|--------|
| 12 | Habilitar acesso SSH no pfSense | ✅ [79.md](79.md) |
| 13 | Regras DMZ p/ Graylog atualizar (update + DNS) | ✅ [80.md](80.md) |
| 14 | Regras de saída (OUTPUT) no iptables do Graylog | ✅ [81.md](81.md) |
| 15 | Configurar resolvedor DNS (Cloudflare) no server | ✅ [82.md](82.md) |
| 16 | Instalar Nginx no server + alias DMZ_UPDATE | ✅ [83.md](83.md) |
| 17 | Proxy reverso no WAF (`site.lab_conf`) | ✅ [84.md](84.md) |
| 18 | ✅ **Regra WAF→server + `site.lab` (hosts) + teste ponta a ponta** | ***este doc*** |
| 19 | ⏭️ Próximos tópicos do Curso 2 | próximo |

---

## 🔗 Conexões com o Projeto

| Tema | Onde aprofundar |
|------|-----------------|
| **Proxy reverso `site.lab_conf` + headers** | [84.md](84.md) |
| **NAT 1:1 do WAF (IP público)** | [76.md](76.md) |
| **Nginx no server backend** | [83.md](83.md) |
| **Block Geral / direção das regras** | [69.md](69.md), [70.md](70.md) |
| **Resolução de nomes (DNS × hosts)** | [66.md](66.md), [82.md](82.md) |
| **Análise de logs (tail -f) / SIEM** | [71.md](71.md), [74.md](74.md) |

---

## 💡 Reflexão Final

> *"Cada lab anterior foi uma peça solta.*
> *Este foi o **encaixe** — o clique em que o navegador digita um nome e, do outro lado, um servidor bem no fundo da rede registra **exatamente quem** o chamou."*

Este lab é o **fecho do arco de proxy reverso**. Um nome (`site.lab`) → resolução local → NAT → WAF (com ModSecurity) → regra de firewall → backend → **log com IP real**. Cada seta é um lab que fizemos, e agora todos funcionam **em cadeia**.

### O que mudou

| Antes | Depois |
|-------|--------|
| `proxy_pass` configurado, mas **502** (sem rota) | Regra WAF→server **libera** o backend |
| `site.lab` não resolvia | `/etc/hosts` aponta para o WAF |
| Fluxo teórico | Aplicação acessível **por nome**, ponta a ponta |
| Log com IP do proxy | Log com **IP real** do cliente ✅ |

### O que vem agora

Com a aplicação publicada por trás do WAF e os logs enriquecidos, a base está pronta para o que **realmente** interessa em segurança defensiva: **simular ataques** contra `site.lab` e ver o **ModSecurity bloqueando** — com tudo pousando no **SIEM** com o IP real do atacante. A portaria está aberta; agora chega o adversário. 🎼🛡️
